#!/bin/bash
echo "Running OCR regression tests..."
python -m pytest tests/test_ocr_regression.py -v --tb=short
if [ $? -ne 0 ]; then
    echo "Tests failed! Push aborted."
    exit 1
fi
echo "All tests passed. Pushing..."
